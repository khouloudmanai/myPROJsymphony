{% extends 'base.html.twig' %}

{% block title %}Voitures{% endblock %}

{% block body %}
    <h1>Voitures</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ voiture.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ voiture.title }}</td>
            </tr>
            <tr>
                <th>Marque</th>
                <td>{{ voiture.marque }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ voiture.description }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ voiture.photo }}</td>
            </tr>
            <tr>
                <th>Prix_journalier</th>
                <td>{{ voiture.prixJournalier }}</td>
            </tr>
            <tr>
                <th>Date_enregistrement</th>
                <td>{{ voiture.dateEnregistrement ? voiture.dateEnregistrement|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Modele</th>
                <td>{{ voiture.modele }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_voitures_index') }}">back to list</a>

    <a href="{{ path('app_voitures_edit', {'id': voiture.id}) }}">edit</a>

    {{ include('voitures/_delete_form.html.twig') }}
{% endblock %}
